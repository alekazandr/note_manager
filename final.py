from add_input import username, title, content, status, created_date, issue_date
from add_list import headlines

note = [
    username,
    title,
    content,
    status,
    created_date,
    issue_date,
    headlines
]

print(note)
